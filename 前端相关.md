## https通过AES和RSA协同工作

1. 服务器发送rsa公钥给浏览器
2. 浏览器随机生成一个aes秘钥，用服务器发送的rsa公钥加密（会话秘钥），并发送给服务器
3. 服务器通过rsa私钥解密，得到与浏览器相同的aes秘钥

## RSA签名

1. 私钥持有者对原始消息通过哈希函数生成消息摘要
2. 利用私钥加密消息摘要生成签名
3. 发送签名和原始消息
4. 接收者通过公钥解密签名，得到消息摘要1（解密失败，说明签名无效）
5. 接收者对原始消息通过哈希函数生成消息摘要2
6. 对比摘要1和摘要2（不相等说明内容被篡改）

## 获取js数据类型

```js
//简略版
Object.prototype.toString.call([]).slice(8,-1).toLowerCase()

function getType(data) {
  // 获取到 "[object Type]"，其中 Type 是 Null、Undefined、Array、Function、Error、Boolean、Number、String、Date、RegExp 等。
  const originType = Object.prototype.toString.call(data)
  // 可以直接截取第8位和倒数第一位，这样就获得了 Null、Undefined、Array、Function、Error、Boolean、Number、String、Date、RegExp 等
  const type = originType.slice(8, -1)
  // 再转小写，得到 null、undefined、array、function 等
  return type.toLowerCase()
}
```



## 单点登录、统一认证、集成登录

1. 单点登录（==SSO==）：只需在认证中心登录一次，就可以访问多个不同的系统，是一种前端用户体验
2. 统一认证：需要搭建一个==中央认证中心（IDP）==，是一种后台架构
3. 集成登录：是一种更为广泛的概念，有多种实现方式（单点登录，密码同步）。实现集成登录这个目标最推荐的方案就是 统一认证+单点登录
4. 为了达到“集成登录”的终极目标，我们通过建设“统一认证”这套基础设施，来向用户提供“单点登录”的无缝体验



## Promise

## Tree Shaking

1. 删除未引用的代码（未被import的代码），减少代码体积

## 浏览器上报日志

1. navigarot.sendBeacon(浏览器关闭也能发送)

## 内存泄漏

1. 全局变量、定时器、闭包、事件监听器、被删除的dom元素但依然被引用、console

## service worker

* 是一个**独立于浏览器主线程**的**脚本**，充当浏览器和网络（或缓存）之间的**代理服务器**。

* 使用场景:

  1. **离线访问**：让 Web 应用在断网时依然显示内容。
  2. **提升加载速度**：缓存常用资源，实现秒开（二次访问时直接从本地缓存加载）。
  3. **消息推送**：实现类似于原生 App 的推送通知功能。
  4. **后台同步**：在网络不稳定时，先将用户操作存储起来，等网络恢复后再发送到服务器。

* ## 文件预览

* ```
  URL.createObjectURL(file);
  const reader = new FileReader();
  
  ```

* <img src="%E5%89%8D%E7%AB%AF%E7%9B%B8%E5%85%B3.assets/image-20260310223818444.png" alt="image-20260310223818444" style="zoom:200%;" />

* ![image-20260310224326694](%E5%89%8D%E7%AB%AF%E7%9B%B8%E5%85%B3.assets/image-20260310224326694.png)

## 接口幂等性

**接口幂等性，就是保证“一次请求”和“多次请求”对资源的影响是一致的。** 它是构建稳定、可靠的系统的基石，主要用来应对网络抖动、重试机制和用户误操作带来的**重复请求**问题。

## vue生命周期

![image-20260311200841784](%E5%89%8D%E7%AB%AF%E7%9B%B8%E5%85%B3.assets/image-20260311200841784.png)

## react生命周期

1. 老版：

![image-20260311201055545](%E5%89%8D%E7%AB%AF%E7%9B%B8%E5%85%B3.assets/image-20260311201055545.png)

2. 新版

![image-20260311201242663](%E5%89%8D%E7%AB%AF%E7%9B%B8%E5%85%B3.assets/image-20260311201242663.png)

## 从浏览器中输入URL这一过程发生了什么

![image-20260311202936920](%E5%89%8D%E7%AB%AF%E7%9B%B8%E5%85%B3.assets/image-20260311202936920.png)

## TCP三次握手四次挥手

![image-20260311205458309](%E5%89%8D%E7%AB%AF%E7%9B%B8%E5%85%B3.assets/image-20260311205458309.png)

## 常见的web攻击

1. ### XSS（跨站脚本攻击）攻击者将恶意脚本（通常是 JavaScript）注入到网页中，当其他用户浏览该页面时，脚本执行，从而窃取信息（如 Cookie、Token）或操作用户账户。

2. ### CSRF（跨站请求伪造）攻击者诱导已登录用户访问恶意网站，利用用户的登录状态，在用户不知情的情况下，以用户的名义向目标网站发送伪造的请求（如修改密码、转账）。

3. ### SQL 注入

4. ### DDoS（分布式拒绝服务攻击）

## 多标签页通信

![image-20260311211925417](%E5%89%8D%E7%AB%AF%E7%9B%B8%E5%85%B3.assets/image-20260311211925417.png)

## 前端性能指标

![image-20260311211925417](%E5%89%8D%E7%AB%AF%E7%9B%B8%E5%85%B3.assets/image-20260312170213802.png)

## web Worker

WebWorker 实际上是运行在浏览器后台的一个单独的线程，因此可以执行一些耗时的操作而不会阻塞主线程。WebWorker 通过与主线程之间传递消息实现通信，这种通信是双向的。WebWorker不能直接访问 DOM，也不能使用像 window 对象这样的浏览器接口对象，但可以使用一些WebWorker 标准接口和 Navigator 对象的部分属性和方法。

## 一些常用的浏览器事件

1. **addEventListener** ： 页面是否活跃：visibilitychange；网络状态：online，offline；页面即将关闭：beforeunload，浏览器窗口大小改变：resize；
2. PerformanceObserver ：是一种JavaScript API，用于监测页面性能指标，如资源加载时间、页面渲染时间等。它可以触发回调函数，以便你收集和分析页面性能数据
3. **MutationObserver**： 是一个在浏览器环境中一种用于监听DOM变化的JavaScript API，它可以用于==监听 DOM 树中指定节点及其子节点的变化==，当节点内容、属性、子节点结构等发生改变时，它可以及时检测到并触发回调函数。这对于前端开发来说非常有用，可以被用来监控 DOM 变化、异步数据加载和自定义组件等场景。
4. `navigator.sendBeacon()`: 方法使得网页可以==异步地将数据发送到服务器==，与页面的卸载过程同时进行，这一点非常重要，因为它允许在不影响用户体验的情况下，安全地结束会话或者发送统计数据
5. requestAnimationFrame：`requestAnimationFrame` 是一个用于==更新动画的 API==，***requestAnimationFrame*** 就是约定在下一次==浏览器刷新前执行的一个定时器==。类似***setTimeout***

## 前端防止重复请求

1. 防抖、节流、Loading、接口幂等性（根据生成的key判断是否已经处理过了）、==AbortController==取消上一次请求（如果上一次请求还没有完成，则取消 abortController.abort，取最后一次数据）

## 前端控制并发数量

1. 数据队列

![image-20260316205634134](%E5%89%8D%E7%AB%AF%E7%9B%B8%E5%85%B3.assets/image-20260316205634134.png)

![image-20260316210409612](%E5%89%8D%E7%AB%AF%E7%9B%B8%E5%85%B3.assets/image-20260316210409612.png)

## canvas 和SVG

![image-20260316212452875](%E5%89%8D%E7%AB%AF%E7%9B%B8%E5%85%B3.assets/image-20260316212452875.png)	

## 前端截图

![image-20260316214506297](%E5%89%8D%E7%AB%AF%E7%9B%B8%E5%85%B3.assets/image-20260316214506297.png)	